<%= form_with(model: relatorio_financeiro) do |form| %>
  <% if relatorio_financeiro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(relatorio_financeiro.errors.count, "error") %> prohibited this relatorio_financeiro from being saved:</h2>

      <ul>
        <% relatorio_financeiro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :transacoes_id, style: "display: block" %>
    <%= form.text_field :transacoes_id , class:"form-control"%>
  </div>

  <div class="form-group text-center mt-4">
    <%= form.submit class: "btn btn-success"%>
  </div>
<% end %>
