<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Exemplo de Tela de Cadastro">
    <title>Sign up</title>
    
    <link rel="shortcut icon" href="favicon/favicon.ico" type="image/x-icon">
    <%= stylesheet_link_tag 'style_devise'%>

    <script src="https://kit.fontawesome.com/6e5ef07d9f.js" crossorigin="anonymous"></script>
</head>

<body>
    <div id="box"><!-- CAIXA -->
        <div id="title">
            <h1>SIGN UP</h1>
        </div>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "signup-form" }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="field">
                <%= f.label :name, "Nome" %><br />
                <%= f.text_field :name, autofocus: true, class: 'form-control', autocomplete: "name", placeholder: "Insira seu nome" %>
            </div>

            <div class="field">
                <%= f.label :email, "Email" %><br />
                <%= f.email_field :email, class: 'form-control', autocomplete: "email", placeholder: "Insira seu email" %>
            </div>

            <div class="field">
                <%= f.label :password, "Senha" %>
                <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> caracteres mínimo)</em>
                <% end %><br />
                <%= f.password_field :password, class: 'form-control', autocomplete: "new-password", placeholder: "Insira sua senha" %>
            </div>

            <div class="field">
                <%= f.label :password_confirmation, "Confirmação de Senha" %><br />
                <%= f.password_field :password_confirmation, class: 'form-control', autocomplete: "new-password", placeholder: "Confirme sua senha" %>
            </div>

            <div class="actions main-action">
                <%= f.submit "SIGN UP", id: "submit" %>
            </div>
        <% end %>
        
        <div class="actions">
            <%= render "devise/shared/links" %>
        </div>
    </div>
</body>
</html>